import * as React from 'react';

export default function About() {
    return (
        <div>This is information about this interface, how to set it up, and what it does. Ideally, it's loaded from a Markdown thing.</div>
    );
};